<% @anouncements.each do |a|%>
  <div class="spacer-10"></div>
  <div class="card">
    <div class="card-body">
      <h2><%= a.title %></h2>
      <small><%= a.created_at.strftime("%a, %d %b %Y") %></small>
      <p><%= a.details %></p>
    </div>
  </div>
<% end %>
